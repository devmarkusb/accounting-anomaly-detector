# Claude Code

Read and follow **[AGENTS.md](./AGENTS.md)** for all repository instructions (build, test, lint, architecture, safety, and review checklist).

Claude-specific notes:

- Prefer editing `src/` and `tests/` only; treat `AGENTS.md` as the single source of truth for commands and constraints.
- Do not push to remotes or publish commits unless the user explicitly asks.
