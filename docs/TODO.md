# Recommended Next Steps

1. Test with your actual bank CSV — run the app, import a real file, tune the profile (column indices, date format, encoding)
2. Category tagging — add a category column to transactions and let users assign categories per-payee for grouping in the summary
3. Keyboard shortcuts for auditing — A/I/X to approve/ignore/anomaly selected rows (makes auditing much faster than right-click)
4. Export — CSV or PDF summary export for a month
5. Chart view — income vs. expenses bar chart per month (PySide6's QChart or matplotlib embedded)
